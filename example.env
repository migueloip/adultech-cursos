# Configuración de Supabase
# Deja estas variables comentadas para usar datos de respaldo en desarrollo local
#NEXT_PUBLIC_SUPABASE_URL= (URL asignada por SUPABASE)
#NEXT_PUBLIC_SUPABASE_ANON_KEY= (Anon Key asignada por SUPABASE)

# Service Role Key para operaciones administrativas (REQUERIDO para el panel de admin)
# Obtén esta clave desde: Supabase Dashboard > Settings > API > service_role (secret)
#SUPABASE_SERVICE_ROLE_KEY= (Service Role Key asignada por SUPABASE)

# Resend API Key (para el envío de emails)
#RESEND_API_KEY= (API Key asignada por RESEND)

# Email Configuration
#CONTACT_EMAIL= (Correo al que se enviara el mensaje)
#FROM_EMAIL= (Email asignado por RESEND)
